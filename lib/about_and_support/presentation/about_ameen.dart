import 'package:flutter/material.dart';

class AboutAmeen extends StatelessWidget {
  const AboutAmeen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
